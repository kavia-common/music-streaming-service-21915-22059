# Monitoring & Logging - Environment Variables Example
# Copy this file to ".env" and customize values as needed.

# Host and port where the FastAPI server will bind.
OBS_HOST=0.0.0.0
OBS_PORT=8083

# API keys for Bearer auth in the format "name1:key1,name2:key2".
# If left empty, auth is disabled (for local/dev only).
# Example: frontend:devkey123,backend:devkey456
OBS_API_KEYS=

# Directory for JSON persistence files (logs.json, metrics.json, alerts.json).
# Relative paths are resolved from the container working directory.
OBS_DATA_DIR=./data

# Enable persistence of in-memory stores to JSON files.
# Accepted true values: "1", "true", "yes" (case-insensitive). Anything else is treated as false.
OBS_ENABLE_PERSISTENCE=true
